#ifndef __tests__
#define __tests__

#include "main.h"
#ifdef RUN_TESTS_ONLY

/**
 * @file tests.h
 *
 * @brief Function declarations for `tests.c`
 *
 * @note The `tests.c` file was exensively used and constantly adjusted during
 * development, but is now basically deprecated and not guaranteed to work
 * correctly.
 */

void run_all_tests();

#endif
#endif
